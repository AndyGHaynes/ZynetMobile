import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    paddingHorizontal: 8,
  },
  hop: {
    flex: 1,
  },
  hops: {
    paddingTop: 12,
    position: 'relative',
  },
});
