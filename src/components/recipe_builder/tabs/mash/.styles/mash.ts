import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  tabContainer: {
    height: '100%',
    paddingHorizontal: 8,
  },
  mashContainer: {
    position: 'relative',
  },
  gradientBoundary: {
    height: 100,
  },
});
