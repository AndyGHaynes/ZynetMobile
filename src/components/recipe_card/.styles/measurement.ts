import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  unit: {
    fontSize: 16,
    width: 28,
    textAlign: 'center',
  },
  value: {
    fontSize: 20,
    textAlign: 'right',
    width: 54,
  },
});
